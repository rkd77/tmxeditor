10 CLEAR 33299 : LOAD "tmxeditor." CODE 33300
15 RANDOMIZE USR 33300
20 CLS : PRINT "S SAVE": PRINT "L LOAD": PRINT "R RETURN"
30 LET a$ = INKEY$
40 IF a$ = "S" OR a$ = "s" THEN GOTO 1000
50 IF a$ = "L" OR a$ = "l" THEN GOTO 2000
60 IF a$ = "R" OR a$ = "r" THEN GOTO 15
70 GOTO 30
1000 CLS : INPUT "PODAJ NAZWE PLIKU "; F$
1010 LET begin = PEEK 30720 + 256 * PEEK 30721
1020 LET length = PEEK 30722 + 256 * PEEK 30723
1030 SAVE f$ CODE begin, length
1040 GOTO 20
2000 CLS : INPUT "PODAJ NAZWE PLIKU "; F$
2010 LET begin = PEEK 30720 + 256 * PEEK 30721
2020 LET length = 20000
2025 REM l dlugosc
2030 LOAD f$ CODE begin, length
2040 LET a = 28 + PEEK 23649 + 256 * PEEK 23650
2050 LET l = PEEK a + 256 * PEEK (a+1)
2060 IF l < length THEN LET length = l
2070 POKE begin + length, 0
2100 GOTO 20
